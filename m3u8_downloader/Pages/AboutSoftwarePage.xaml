<UserControl
    x:Class="m3u8_downloader.Pages.AboutSoftwarePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="400"
    d:DesignWidth="600"
    mc:Ignorable="d">

    <Border
        Margin="10"
        Background="White"
        CornerRadius="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="1" />
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                Padding="15"
                FontSize="18"
                FontWeight="Bold"
                Foreground="{DynamicResource AppTextBrush}"
                Text="关于软件" />

            <Grid
                Grid.Row="1"
                Margin="15,0"
                Background="{DynamicResource AppBorderBrush}" />

            <Grid
                Grid.Row="2"
                HorizontalAlignment="Center"
                VerticalAlignment="Top">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Image
                    Grid.Row="0"
                    Width="128"
                    Height="128"
                    Margin="15"
                    Source="/Images/application.png" />

                <TextBlock
                    Grid.Row="1"
                    HorizontalAlignment="Center"
                    FontSize="36"
                    Foreground="{DynamicResource AppThemeBrush}"
                    Text="M3U8资源下载器" />

                <TextBlock
                    Grid.Row="2"
                    Margin="0,10"
                    HorizontalAlignment="Center"
                    FontSize="20"
                    Foreground="{DynamicResource AppThemeBrush}"
                    Text="版本：v1.0.0.0" />
            </Grid>

            <TextBlock
                Grid.Row="3"
                Margin="10"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="16"
                Foreground="{DynamicResource AppTextBrush}"
                Text="Copyright © CoderPeng 2025 All rights reserved." />
        </Grid>
    </Border>
</UserControl>